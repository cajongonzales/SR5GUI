from setuptools import setup

setup(
   name='SR5GUI',
   version='0.0',
   description='A GUI to reorganize the rules',
   author='Cajon Gonzales',
   author_email='cajongonzales@gmail.com',
   packages=['SR5GUI'],  # would be the same as name
   #install_requires=['wheel', 'bar', 'greek'], #external packages acting as dependencies
)
